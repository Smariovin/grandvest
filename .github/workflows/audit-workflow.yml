name: Audit n8n Workflow

on:
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run audit
        env:
          N8N_URL: http://85.239.61.157:5678
          N8N_API_KEY: ${{ secrets.N8N_API_KEY }}
        run: python3 scripts/audit_workflow.py
