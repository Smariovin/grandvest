name: Master Diagnostic Fix
on:
  workflow_dispatch:
jobs:
  fix:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" | base64 -d > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H 85.239.61.157 >> ~/.ssh/known_hosts
      - name: Deep diagnosis via SSH
        env:
          GH_PAT: ${{ secrets.GH_TOKEN }}
          TG_BOT: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_CHAT: "5340000158"
        run: |
          scp -o StrictHostKeyChecking=no scripts/deep_diagnosis.py root@85.239.61.157:/root/deep_diag.py
          ssh -o StrictHostKeyChecking=no root@85.239.61.157 "GH_PAT=$GH_PAT TG_BOT=$TG_BOT TG_CHAT=$TG_CHAT python3 /root/deep_diag.py 2>&1"
